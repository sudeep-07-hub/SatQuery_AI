# Lightweight backend for Render's free tier (512 MB RAM, 0.1 CPU).
# No LLM or vision-language model weights: tool selection uses the Tool Registry rules and change
# detection uses the classical engine. The full stack uses requirements.txt instead.

# CPU-only PyTorch (the default PyPI wheel pulls in multi-GB CUDA libraries)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4

fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0
numpy>=1.26
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.6.0
Pillow>=10.0.0
geojson>=3.1.0
reportlab>=4.0
requests>=2.31
