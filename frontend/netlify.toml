[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: /assistant and other deep links are served by index.html.
# Non-forced (status 200) rules only apply when no static file matches, so assets are unaffected.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
