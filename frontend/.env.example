# Base URL of the SatQuery FastAPI backend (no trailing slash needed).
# Read at build time by Vite: set it in the Vercel/Netlify project settings for deployments.
# A page served over https:// must use an https:// backend URL (browsers block mixed content).
VITE_API_BASE=http://localhost:8000
